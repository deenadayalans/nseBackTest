# OEH Backtest & Live Alerter — Python 3.11–3.13
# Install:  pip install -r requirements.txt

kiteconnect==5.2.0        # Zerodha Kite Connect API + WebSocket
pandas==3.0.3
numpy==2.2.6
pyarrow==24.0.0           # Parquet file storage
scipy==1.17.1             # Black-Scholes option pricing
requests==2.34.2          # WhatsApp / Telegram HTTP alerts
python-dotenv==1.2.2      # Load .env config
pandas-ta==0.4.71b0       # Technical indicators (SuperTrend, EMA etc.)
backtrader==1.9.78.123    # Backtesting engine (legacy, kept for compatibility)
rich==15.0.0              # Pretty terminal output
schedule==1.2.2           # Scheduling helpers
tqdm==4.67.3              # Progress bars
